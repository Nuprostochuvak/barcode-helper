import 'package:flutter/material.dart';

class WirelessConfigPage extends StatelessWidget {
  const WirelessConfigPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
